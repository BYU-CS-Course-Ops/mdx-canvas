<div>
<page
    id="demo page"
    title="{{ demo_page_title }}">

# Comprehensive Demo Page

This is a demo page for testing all possible content types supported in Canvas.

## 1. Text Formatting

Here we have **bold text**, *italic text*, ***bold and italic***, and ~~strikethrough text~~. You can also use `inline code` within sentences.

## 2. HTML Elements

<div style="background-color: #f0f0f0; padding: 10px; border-radius: 5px;">
    <h3>HTML Section</h3>
    <p>This is <strong>HTML content</strong> with <em>styled elements</em>.</p>
    <p>You can include <span style="color: blue;">colored text</span> and other HTML features.</p>
</div>

<br />

<blockquote>
    This is a blockquote using HTML tags. It can contain multiple paragraphs and other content.
</blockquote>

## 3. Code Blocks

### Python Example

```python
def fibonacci(n):
    """Calculate the nth Fibonacci number."""
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

# Calculate the first 10 Fibonacci numbers
for i in range(10):
    print(f"F({i}) = {fibonacci(i)}")
```

### JavaScript Example

```javascript
// Array operations and functional programming
const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(n => n * 2);
const sum = doubled.reduce((acc, val) => acc + val, 0);

console.log(`Sum of doubled numbers: ${sum}`);
```

### HTML/CSS Example

```html
<!DOCTYPE html>
<html>
<head>
    <style>
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Hello, World!</h1>
    </div>
</body>
</html>
```

### SQL Example

```sql
SELECT students.name, courses.title, enrollments.grade
FROM students
INNER JOIN enrollments ON students.id = enrollments.student_id
INNER JOIN courses ON enrollments.course_id = courses.id
WHERE enrollments.grade >= 90
ORDER BY students.name;
```

## 4. Lists with Subsections

### Unordered Lists

- **Programming Languages**
    - Python
        - Django
        - Flask
        - FastAPI
    - JavaScript
        - React
        - Vue
        - Angular
    - Java
        - Spring Boot
        - Hibernate
- **Development Tools**
    - Version Control
        - Git
        - GitHub
        - GitLab
    - IDEs
        - VS Code
        - PyCharm
        - IntelliJ IDEA
- **Databases**
    - Relational
        - PostgreSQL
        - MySQL
        - SQLite
    - NoSQL
        - MongoDB
        - Redis
        - Cassandra

### Ordered Lists

1. **Setup Development Environment**
    1. Install Python 3.x
    2. Install required packages using `pip install -r requirements.txt`
    3. Configure environment variables
2. **Create Project Structure**
    1. Initialize repository with `git init`
    2. Create project directories
        - src/
        - tests/
        - docs/
    3. Add `.gitignore` file
3. **Implement Core Features**
    1. Design database schema
    2. Create API endpoints
        - GET `/api/users`
        - POST `/api/users`
        - PUT `/api/users/:id`
        - DELETE `/api/users/:id`
    3. Write unit tests

## 5. Links

Here are various types of links:

- External link: [Canvas LMS Official Website](https://www.instructure.com/canvas/)
- Educational resource: [MDN Web Docs](https://developer.mozilla.org/)
- Documentation: [Python Official Documentation](https://docs.python.org/3/)
- Academic resource: [MIT OpenCourseWare](https://ocw.mit.edu/)

You can also reference links inline like this: Visit [GitHub](https://github.com) for code repositories.

## 6. Images

Here's an example of embedding images:

![Python Logo](https://www.python.org/static/community_logos/python-logo-generic.svg)

<img src="https://upload.wikimedia.org/wikipedia/commons/6/6a/JavaScript-logo.png" alt="JavaScript Logo" width="200" />

## 7. LaTeX Equations

### Inline Mathematics

The quadratic formula is \(x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}\) and Einstein's famous equation is \(E = mc^2\).

The Pythagorean theorem states that \(a^2 + b^2 = c^2\) for right triangles.

### Block Mathematics

Here's the integral definition of the natural logarithm:

\[
\ln(x) = \int_1^x \frac{1}{t} \, dt
\]

The Gaussian (normal) distribution probability density function:

\[
f(x | \mu, \sigma^2) = \frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}
\]

Maxwell's equations in differential form:

\[
\begin{align}
\nabla \cdot \mathbf{E} &= \frac{\rho}{\epsilon_0} \\
\nabla \cdot \mathbf{B} &= 0 \\
\nabla \times \mathbf{E} &= -\frac{\partial \mathbf{B}}{\partial t} \\
\nabla \times \mathbf{B} &= \mu_0\mathbf{J} + \mu_0\epsilon_0\frac{\partial \mathbf{E}}{\partial t}
\end{align}
\]

Euler's identity, often called the most beautiful equation in mathematics:

\[
e^{i\pi} + 1 = 0
\]

## 8. Tables

| Language | Paradigm | Typing | Use Case |
|----------|----------|--------|----------|
| Python | Multi-paradigm | Dynamic | Data Science, Web Development |
| Java | Object-Oriented | Static | Enterprise Applications |
| JavaScript | Multi-paradigm | Dynamic | Web Development |
| C++ | Multi-paradigm | Static | Systems Programming |
| Haskell | Functional | Static | Academic, Financial Systems |

## 9. Mixed Content Example

When working with algorithms, we often need to analyze their **time complexity**. For example, the binary search algorithm has a time complexity of \(O(\log n)\), which is much better than the linear search's \(O(n)\) complexity.

Here's the implementation:

```python
def binary_search(arr, target):
    """Binary search implementation with O(log n) complexity."""
    left, right = 0, len(arr) - 1

    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1

    return -1
```

To verify the time complexity mathematically:
- At each step, we eliminate half of the remaining elements
- If we start with \(n\) elements, after \(k\) steps we have \(\frac{n}{2^k}\) elements
- When \(\frac{n}{2^k} = 1\), we're done
- Solving: \(2^k = n\), therefore \(k = \log_2 n\)

\[
T(n) = T\left(\frac{n}{2}\right) + O(1) \implies T(n) = O(\log n)
\]

## Summary

This demo page contains:
1. Text formatting (bold, italic, strikethrough, inline code)
2. HTML elements with styling
3. Multiple code blocks (Python, JavaScript, HTML/CSS, SQL)
4. Nested lists (both ordered and unordered)
5. Various types of links
6. Image embedding
7. LaTeX equations (both inline and block)
8. Tables
9. Mixed content combining all elements

For questions or issues, please contact your course instructor.

</page>

<page title="Another demo">

    This is yet another demo. See <course-link type="page" id="demo page"/>.

    And this is the syllabus: <course-link type="syllabus" id="syllabus"/>

</page>
</div>