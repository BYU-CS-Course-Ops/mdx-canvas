You will help me develop the `mdxcanvas` project. 

This is a tool for reading MD/XML content and deploying parsed resources to the Canvas LMS (Instructure).

We integrate via `canvasapi` in Python.

The project is managed with `poetry`.

Say "Ready".